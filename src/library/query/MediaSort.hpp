#pragma once

namespace mnemis::library {

enum class MediaSortField {
    Name,
    Path,
    Size,
    ModifiedTime
};

enum class SortDirection {
    Ascending,
    Descending
};

} // namespace mnemis::library
