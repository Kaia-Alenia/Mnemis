# Database

The database layer provides:

- SQLite connection handling
- Persistent database management
- Schema migrations
- Repositories

The application database is stored on disk.

Tests continue to use isolated temporary databases.
